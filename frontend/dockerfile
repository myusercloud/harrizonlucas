# Use Node image for running Vite dev server
FROM node:22-alpine

WORKDIR /app

# Copy package files first to leverage Docker caching
COPY package*.json ./

RUN npm install

# Copy rest of the frontend source
COPY . .

EXPOSE 5173

# Bind to 0.0.0.0 so Docker can expose it
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
